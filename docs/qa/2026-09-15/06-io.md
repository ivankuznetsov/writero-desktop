# QA06 — import/export and Markdown

Worktree: `writero-desktop.worktrees/qa-io`, branch `qa/15-sep-io`; dependency `66bfdbe` (QA02 storage fix, not recounted).
Wiki research: writero search returned server-only documentation, no applicable desktop contracts; master returned no results. CONTRIBUTING requires golden roundtrips and recovery evidence.

| ID | Confirmed root cause / reproduction | Fix and evidence |
|---|---|---|
| QA06-001 | Code parse joins based on empty content and trims all blocks, destroying leading indentation and blank lines and dropping empty code. `codeRoundTrip` failed indentation, blank-lines and empty fixtures. | Preserve code lines exactly, retain empty code. Three golden fixtures now pass. |
| QA06-002 | Fence parser only recognizes exactly three backticks and treats info-bearing fence lines as closers. `closingFenceMustNotHaveInfoString`, `recognizesTildeAndLongFences` failed (lost code/type). | Track opening delimiter and length; only matching delimiter at least that long with no info closes. Both fixtures pass. |
| QA06-003 | Serializer always emits three backticks, so embedded triple backticks terminate exported code. `codeRoundTrip(embedded-fence)` returned two blocks. | Choose delimiter longer than embedded runs; roundtrip now one identical code block. |
| QA06-004 | Markdown image export had no matching import path and did not escape alt/destination syntax. `mediaRoundTrip` reimported Unicode image as Text. | Parse standalone images and escape brackets/delimit destinations. Golden Unicode/bracket/space/parenthesis roundtrip passes. |

Markdown red: 7 failing cases; green: 18/18 cases. Positive control: divider immediately after list remains a separate block (passed before fix, not a bug).

| ID | Confirmed root cause / reproduction | Fix and evidence |
|---|---|---|
| QA06-005 | PDF export only checked destination existence, reporting success when destination was an existing directory despite QPainter failure. | Open QSaveFile before rendering, commit only after writer flush; directory rejection and valid PDF header pass. |
| QA06-006 | Bundle import ignored format version, accepting version 999 using v1 semantics. | Reject unsupported version; data-row red→green. |
| QA06-007 | Bundle import silently coerced malformed block arrays, duplicate IDs and unknown types, losing data on later save/remap. | Validate manifest container types and block identities/types before constructing objects; three failing rows now pass. |
| QA06-008 | Bundle media validation checked hash bytes but not declared size or resolution of block references; absent referenced blobs became blank images. | Validate sizes, uniqueness, and every block SHA resolves; missing-media/byte-size/media-reference rows red→green. |
| QA06-009 | Import populated caller output incrementally, leaving it overwritten on later missing/tampered blob failure. | Parse into temporary BundleContents, publish only on total success. Sentinel-output regression red→green. |
| QA06-010 | Export used unvalidated media hash as path, allowing `../../escaped` to write outside bundle. | Validate digest syntax/content/size before directory creation; traversal regression red→green. |

Document I/O red: 10 failing cases; green: 19/19. Existing HTML escaping, PDF header, media tamper and import traversal controls pass.

| ID | Confirmed root cause / reproduction | Fix and evidence |
|---|---|---|
| QA06-011 | Workspace imported history after document transaction committed and ignored insert failure. SQLite abort trigger reproduced success despite rejected history. | Pass imported revisions into existing save transaction, rollback and return SQL error on failure. Fault-injection regression now passes; recovery import succeeds after trigger removal. |
| QA06-012 | Exported history is newest-first but import assigned IDs in that same order, reversing equal-timestamp revisions when listed by ID descending. | Insert imported history oldest-first. Two equal-time revisions red (`first` shown newest)→green (`second` newest). |
| QA06-013 | Markdown file import retained relative image paths, so rendering resolved against application location instead of imported file directory. | Resolve media URLs against source file URL. Relative local image path red→green pending final combined run. |
| QA06-014 | UTF-8 import silently replaced invalid bytes, allowing corrupted text to replace the document without error. | Decode with error-aware QStringDecoder and reject read/decode errors. Invalid byte fixture red→green pending final combined run. |

UTF-8 BOM positive control passed before changes and is not counted. API service is not involved in these local serialization/storage paths; no API E2E claims made by QA06.

## Final verification and handoff

- Fix commit: `2e87490` on `qa/15-sep-io`; parent should cherry-pick this commit only (dependency `66bfdbe` already represents QA02 work).
- Final targeted run: `cmake --build build -j2 --target tst_documentio tst_markdown tst_workspace tst_workspacestore` then `ctest --test-dir build -R 'tst_(documentio|markdown|workspace|workspacestore)$' --output-on-failure`: **4/4 suites passed**, 1.87 seconds.
- Stress: **2,000 deterministic randomized code roundtrips** (Unicode, tabs/newlines, fence characters, brackets, lengths 0–511) pass. Table/link/nested-list golden roundtrip and real PNG inclusion in PDF pass.
- QA06-013 and QA06-014 are green in final combined run. Truncated UTF-8 EOF also reproduced a failing case and is now rejected using stateless decoding; counted within QA06-014, not separately.
- **14 distinct root bugs fixed**; 0 fabricated or duplicate QA02 counts. No source/API credentials used. Report contains synthetic fixtures only.
- Self-review: full implementation diff read; `git diff --check` clean. No push; parent owns integrated review/PR.

Residual contract gaps, not claimed fixed or counted: revision schema carries no historical media ID, and current bundles include current media only; historical media restoration requires a broader schema/restore decision. An aborted import may leave unreferenced content-addressed media cached; document/history state now rolls back atomically. API gateway integration is covered by other agents; these checks exercise local desktop IO and SQLite paths.
