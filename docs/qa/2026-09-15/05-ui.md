# QA05 native QML shell and dialogs

- Scope: actual Qt Quick components, main window, dialogs, provider settings, AI results, native keyboard/mouse. Branch `qa/15-sep-ui`; pristine baseline `149c6f6`.
- Skills: ce-debug return-to-caller + wiki-researcher; no project wiki found, master wiki has no applicable native Qt patterns.
- In progress: building Qt Quick Test harness; full-shell loading and dialog regressions being added. Synthetic data only, isolated XDG paths.

| ID | Reproduction and root cause | Evidence | Status |
| --- | --- | --- | --- |
| QA05-001 | Start full native shell: Main references ShareLinkDialog, but QML module omits its resource/type registration | `test_mainStarts`: ShareLinkDialog is not a type | registration fixed |
| QA05-002 | After registering share dialog, create it: TextField declares unsupported `onActivated`; QML compilation aborts and shell still cannot start | `test_shareDialogLoads`, stage2 log: Cannot assign to non-existent property onActivated | use active-focus notification |
| QA05-003 | Display AI output >220 chars or revision >80 chars: QML JavaScript calls QString-only `.left()` method; throws, leaving preview blank. Same root occurs in cloud revision >70 chars | long result and local revision tests both reproduce TypeError; root fixed at all three sites | JavaScript slice applied |
| QA05-004 | Use keyboard Down to switch OpenRouter to OpenAI-compatible then Ollama: endpoint remains OpenRouter because handler compares to *new* default rather than any prior preset | provider test actual openrouter endpoint, expected OpenAI endpoint | preset switch logic fixed, custom URL retained |
| QA05-005 | Fail sign-in against refused loopback endpoint, then retry: Connect click imperatively overwrites account status binding; subsequent account changes no longer update label | `test_accountStatusRecoversAfterFailedSignIn` retry status assertion fails | status remains declaratively bound to connected/error state |
| QA05-006 | Open conflict panel before a cloud conflict arrives; save synthetic conflict then emit real engine conflict notification: list remains empty. `conflictList()` invokable has no automatic QML notification dependencies | `test_conflictPanelUpdatesAfterCloudChange` list count actual 0, expected 1 | explicit engine signal/on-open refresh |

## Final verification

- All **6** confirmed root causes fixed. Related long-preview failures counted once despite three affected sites.
- `cmake --build build --target tst_ui -j2`: passed.
- `QT_QPA_PLATFORM=offscreen build/tests/tst_ui -input tests/ui`: **19 passed, 0 failed**, including 8 preexisting editor scenarios and 7 new shell scenarios (plus suite setup/cleanup).
- `QT_QPA_PLATFORM=wayland build/tests/tst_ui -input tests/ui/tst_shell.qml`: **9 passed, 0 failed** on the live Omarchy Wayland compositor with isolated synthetic workspace/credentials. Programmatic native pass, not claimed as human manual testing.
- Screenshot captured and visually inspected: `05-native-shell-wayland.png` (native content area; header/footer excluded by QQuickItem capture). Offscreen comparison: `05-native-shell.png`.
- Actual provider selection uses native keyboard events and checks custom endpoints remain intact. Account failure uses refused localhost endpoint; no external sign-in or private credentials used. Conflict test seeds SQLite and delivers actual engine notification.
- API gateway 45105 was allocated but unused by this UI assignment; real API integration belongs to other assignments. No claims of 200 bugs or broad untested feature coverage.
- Residual observations, not counted as confirmed user-impact defects: full shell emits AiPanel provider ComboBox model binding-loop warnings, and StandardKey single-sequence warnings (alternative keybindings may be omitted). Further runtime tracing needed. Hosted catalog behavior, large conflict histories, minimum-window dialog layouts, drag/drop and complete OAuth success flow not covered here.
- Report remains outside commit; parent owns aggregate QA report and shipping.
